// pegar o elemento canvas pelo id
const canvas = document.getElementById('jogo2d')

// inicializar o canvas
const ctx = canvas.getContext('2d')

// criar função loop que executa 60 vezes por segundo
